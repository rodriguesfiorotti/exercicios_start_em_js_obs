export function dobro (x) {
    let dobrado = (x * 2)
    return dobrado
}

export function somar (x, y) {
    let somatorio = (x + y)
    return somatorio
}

export function subtrair (x, y) {
    let subtracao = (x - y)
    return subtracao
}